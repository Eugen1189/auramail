# ⚡ Швидке виправлення конфлікту портів Redis

## 🔍 Проблема

Помилка: `Bind for 0.0.0.0:6379 failed: port is already allocated`

## ✅ Швидке рішення

### Варіант 1: Зупинити старі Docker контейнери (РЕКОМЕНДОВАНО)

```bash
# Зупинити всі контейнери AuraMail
docker compose down

# Перевірити, чи є "завислі" контейнери
docker ps -a

# Видалити старі контейнери (якщо потрібно)
docker compose rm -f

# Запустити знову
docker compose up -d
```

### Варіант 2: Зупинити процес на порту 6379

```powershell
# Знайти процес на порту 6379
netstat -ano | findstr :6379

# Зупинити процес (замініть PID на ваш)
taskkill /PID <PID> /F
```

### Варіант 3: Використати інший порт

Якщо порт 6380 теж зайнятий, змініть на інший (наприклад, 6381):

**Створіть або оновіть `.env` файл:**

```env
REDIS_PORT=6381
```

Потім запустіть:

```bash
docker compose up -d
```

## 🔧 Перевірка

Після виправлення перевірте:

```bash
# Перевірити статус контейнерів
docker compose ps

# Перевірити логи Redis
docker compose logs redis

# Перевірити, що порт вільний
netstat -ano | findstr :6380
```

## ✅ Очікуваний результат

Ви повинні побачити:

```
✅ redis container started successfully
✅ web container connected to redis
✅ worker container connected to redis
```

## 📝 Примітка

**Важливо:** У вашому `docker-compose.yml` вже налаштовано порт 6380 замість 6379. Проблема може бути в тому, що:
1. Старий контейнер все ще працює
2. Локальний Redis запущений на Windows
3. Інший Docker контейнер використовує порт 6380

---

**Якщо проблема залишається**, дивіться детальний гайд: [DOCKER_TROUBLESHOOTING.md](DOCKER_TROUBLESHOOTING.md)

