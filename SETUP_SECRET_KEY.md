# 🔐 Встановлення FLASK_SECRET_KEY

## Крок 1: Оновлення config.py ✅

Файл `config.py` вже оновлено - дефолтний ключ видалено. Тепер `FLASK_SECRET_KEY` є обов'язковим.

## Крок 2: Генерація нового SECRET KEY

Запустіть скрипт для автоматичного створення/оновлення `.env` файлу з новим ключем:

```powershell
python create_env.py
```

Або згенеруйте ключ вручну:

```powershell
python -c "import secrets; print(secrets.token_hex(32))"
```

Потім додайте його до `.env` файлу:

```
FLASK_SECRET_KEY=ваш_згенерований_ключ_тут
```

## Крок 3: Перевірка .env файлу

Переконайтеся, що `.env` файл містить:

```env
# AuraMail Environment Variables
# DO NOT COMMIT THIS FILE TO GIT!

# Flask Secret Key (REQUIRED for session security)
FLASK_SECRET_KEY=ваш_згенерований_ключ_тут

# Інші змінні...
GEMINI_API_KEY=ваш_api_ключ
REDIS_URL=redis://localhost:6379/0
DATABASE_URL=sqlite:///auramail.db
BASE_URI=https://127.0.0.1:5000
DEBUG=False
```

## ⚠️ Важливо

1. **НЕ коммітьте `.env` файл у Git!** Переконайтеся, що він є в `.gitignore`
2. **Використовуйте один і той самий ключ** для всіх частин програми (server та worker)
3. **Для production** використовуйте окремий, унікальний ключ (не той самий, що для розробки)

## Перевірка

Після встановлення ключа спробуйте запустити сервер:

```powershell
python server.py
```

Якщо ключ не встановлено, ви отримаєте помилку від `python-decouple` про відсутність змінної.

