# 📤 Інструкція: Збереження всіх змін до GitHub

## 🚀 Швидкий спосіб (рекомендовано)

### Варіант 1: Batch файл (Windows CMD)
Просто подвійний клік або запустіть у командному рядку:
```cmd
git_push_all.bat
```

### Варіант 2: PowerShell скрипт
Запустіть у PowerShell:
```powershell
.\git_push_all.ps1
```

---

## 📝 Ручне виконання (якщо скрипти не працюють)

Виконайте команди по черзі:

### 1. Додати всі файли
```bash
git add -A
```

### 2. Перевірити, що додано
```bash
git status
```

### 3. Створити коміт
```bash
git commit -m "feat: Complete AuraMail project with 66% test coverage and CI/CD"
```

### 4. Перевірити/додати remote
```bash
git remote -v
```
Якщо remote не існує:
```bash
git remote add origin https://github.com/Eugen1189/auramail.git
```
Якщо існує, але з неправильним URL:
```bash
git remote set-url origin https://github.com/Eugen1189/auramail.git
```

### 5. Встановити гілку main
```bash
git branch -M main
```

### 6. Відправити на GitHub
```bash
git push -u origin main
```

---

## ⚠️ Автентифікація GitHub

GitHub **не приймає паролі** для git push. Вам потрібен **Personal Access Token (PAT)**:

### Як створити токен:

1. Відкрийте: https://github.com/settings/tokens
2. Натисніть **"Generate new token"** → **"Generate new token (classic)"**
3. Дайте назву: `AuraMail Project`
4. Виберіть права доступу:
   - ✅ `repo` (повний доступ до репозиторіїв)
5. Натисніть **"Generate token"**
6. **СКОПІЮЙТЕ ТОКЕН** (він показується тільки один раз!)

### Використання токену:

При `git push` коли запитує:
- **Username:** ваш GitHub username
- **Password:** вставте скопійований Personal Access Token (не пароль!)

---

## 🔍 Діагностика проблем

### Перевірити, чи всі файли додані:
```bash
git status --short
```

### Перевірити, скільки файлів у git:
```bash
git ls-files | wc -l
```

### Переглянути останні коміти:
```bash
git log --oneline -5
```

### Перевірити налаштування Git:
```bash
git config --list
```

---

## 📋 Список файлів, які НЕ будуть додані (через .gitignore):

- `.env` - конфіденційні дані
- `client_secret.json` - OAuth credentials
- `*.db` - бази даних
- `*.log` - логи
- `__pycache__/` - кеш Python
- `.pytest_cache/` - кеш тестів
- `htmlcov/` - звіти покриття
- `instance/` - локальні дані Flask

**Це нормально і правильно!** Ці файли не повинні бути в репозиторії.

---

## ✅ Перевірка після push

Після успішного push:
1. Відкрийте: https://github.com/Eugen1189/auramail
2. Перевірте, що всі файли присутні
3. GitHub Actions автоматично запуститься

