# 🔧 Виправлення конфлікту портів Redis

**Проблема:** Порт 6379 вже зайнятий локальним Redis на Windows.

**Рішення:** Змінено зовнішній порт Redis в Docker на **6380**.

---

## ✅ Що змінено

У файлі `docker-compose.yml`:
- **Було:** `"${REDIS_PORT:-6379}:6379"`
- **Стало:** `"${REDIS_PORT:-6380}:6379"`

**Важливо:** Внутрішній порт контейнера залишився **6379** (не змінюється), змінився тільки зовнішній порт на **6380**.

---

## 🚀 Як використовувати

### Варіант 1: Використати дефолтний порт 6380

Просто запустіть:
```bash
docker compose up -d
```

Redis буде доступний на `localhost:6380` ззовні, але всередині Docker мережі залишається на `6379`.

### Варіант 2: Вказати інший порт через .env

Створіть `.env` файл (якщо його немає):
```env
REDIS_PORT=6381
```

Або будь-який інший вільний порт.

---

## 📝 Примітки

1. **Внутрішній порт не змінюється:**
   - Всі сервіси всередині Docker (web, worker) підключаються до Redis через внутрішню мережу на порту `6379`
   - Змінився тільки зовнішній порт для доступу з хоста

2. **Локальний Redis:**
   - Якщо у вас запущений локальний Redis на Windows, він продовжить працювати на порту 6379
   - Docker Redis буде на порту 6380
   - Конфлікту не буде!

3. **Підключення з хоста:**
   - Якщо потрібно підключитися до Redis з хоста (наприклад, для тестування), використовуйте:
   ```bash
   redis-cli -p 6380
   ```

---

## ✅ Перевірка

Після запуску перевірте:
```bash
# Перевірити, що контейнер запущений
docker ps | grep redis

# Перевірити, що порт 6380 слухається
netstat -ano | findstr :6380

# Перевірити підключення до Redis
docker exec -it auramail-redis redis-cli ping
# Має повернути: PONG
```

---

**Статус:** ✅ Виправлено  
**Дата:** 2025-12-25

