# 🎨 Gmail Label Colors Configuration

## Огляд

AuraMail використовує кольорове кодування міток Gmail для кращої візуальної організації. Кожна категорія має свій унікальний колір.

## Конфігурація

Кольори налаштовані в `config.py` через `LABEL_COLOR_MAP`:

```python
LABEL_COLOR_MAP = {
    'IMPORTANT': '#4d96b9',      # Blue
    'ACTION_REQUIRED': '#f83a22', # Red
    'BILLS_INVOICES': '#ff9500',  # Orange
    'PERSONAL': '#28a745',        # Green
    'PROJECT': '#6f42c1',         # Purple
    'REVIEW': '#ffc107',          # Yellow
    'NEWSLETTER': '#6c757d',      # Gray
    'SOCIAL': '#17a2b8',          # Cyan
    'SPAM': '#dc3545',            # Red
    'MARKETING': '#adb5bd',       # Light gray
    'DEFAULT': '#4d96b9'          # Default blue
}
```

## Як це працює

1. **Автоматичне визначення кольору:**
   - При створенні мітки AI система визначає категорію з назви мітки або з параметра `category`
   - Формат міток: `AI_CATEGORY_NAME` (наприклад, `AI_BILLS`, `AI_IMPORTANT`)

2. **Приклад використання:**
   ```python
   # З category параметром (рекомендовано)
   label_id = get_or_create_label_id(service, 'AI_BILLS', label_cache, category='BILLS_INVOICES')
   # Використає оранжевий колір (#ff9500)
   
   # Без category (автоматичне визначення з назви)
   label_id = get_or_create_label_id(service, 'AI_IMPORTANT_EMAILS', label_cache)
   # Використає синій колір (#4d96b9) бо "IMPORTANT" знайдено в назві
   ```

3. **Формат кольорів:**
   - Використовуються HEX коди кольорів у форматі `#RRGGBB`
   - Всі кольори валідні для Gmail API
   - Текст завжди білий (`#ffffff`) для кращого контрасту

## Додавання нових кольорів

Щоб додати новий колір для категорії:

1. Додайте запис у `LABEL_COLOR_MAP` в `config.py`:
   ```python
   'NEW_CATEGORY': '#hexcode',  # Ваш HEX код
   ```

2. Перезапустіть додаток

## Валідні HEX коди для Gmail API

Gmail API приймає будь-які валідні HEX коди. Рекомендується використовувати:
- Яскраві, насичені кольори для кращої видимості
- Контрастні кольори для розрізнення категорій
- Стандартну палітру для консистентності

## Приклад результатів

- `AI_BILLS` → Оранжевий фон (#ff9500)
- `AI_IMPORTANT` → Синій фон (#4d96b9)
- `AI_SPAM` → Червоний фон (#dc3545)
- `AI_REVIEW` → Жовтий фон (#ffc107)

Всі мітки мають білий текст для кращого читання.

